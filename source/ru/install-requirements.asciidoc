[[install-requirements]]

=== Основы: Требования к серверу

[role="summary"]
Обзор требований к серверу для установки и работы ядра Drupal.

(((Требования к установке,обзор)))
(((Требования к установке,место на диске)))
(((Требования к установке,веб-сервер)))
(((Требования к установке,база данных)))
(((Требования к установке,PHP)))
(((Установка,ядро Drupal)))
(((Ядро Drupal,требования к установке)))
(((Место на диске,требования к установке)))
(((Веб-сервер,требования к установке)))
(((Apache веб-сервер,требования к версии)))
(((Nginx веб-сервер,требования к версии)))
(((Microsoft IIS веб-сервер,требования к версии)))
(((База данных,требования к версии)))
(((MySQL база данных,требования к версии)))
(((PostgreSQL база данных,требования к версии)))
(((SQLite база данных,требования к версии)))
(((PHP,требования к версии)))

==== Каковы требования для запуска ядра Drupal?

===== Место на диске

Общий объём дискового пространства, необходимого для вашего сайта, зависит от его размера и структуры. Базовые файлы ядра Drupal занимают около 100 МБ на сервере. Дополнительные модули и темы потребуют больше места, также необходима область для хранения медиафайлов, резервных копий и других файлов, загружаемых или генерируемых сайтом. База данных также занимает место на диске, но, как правило, это другая папка или даже отдельный сервер.

===== PHP

**PHP 8.3 или выше**. Минимальный лимит памяти PHP (memory_limit) — 64 МБ, однако для современных сайтов, особенно с дополнительными модулями или при использовании Composer и других CLI-инструментов на PHP, рекомендуется устанавливать лимит памяти значительно выше (128–256 МБ и более).

Для работы Drupal требуются определённые PHP-расширения. Их точный список зависит от способа установки ядра и используемых вами модулей. Как правило, на хостингах все необходимые расширения уже установлены. При самостоятельной установке вы получите сообщение об ошибке, если какого-либо расширения не хватает. Список всех необходимых расширений — по ссылке:
https://www.drupal.org/docs/getting-started/system-requirements/php-requirements[PHP requirements].

===== Веб-сервер

Apache (рекомендуется)::
  Наиболее часто используемый веб-сервер. Drupal работает на **Apache 2.4.7** и выше (UNIX/Linux, OS X или Windows) с установленным и включённым модулем mod_rewrite. В конфигурации виртуального хоста Apache должно быть указано _AllowOverride All_ для работы с _.htaccess_.

PHP-встроенный сервер::
  Для временного локального демо-сайта вы можете запускать сайт только с помощью встроенного в PHP сервера, без установки полноценного веб-сервера.

Nginx::
  Поддерживается Nginx **0.7** и выше (UNIX/Linux, OS X или Windows). Должен быть установлен и включён модуль ngx_http_rewrite_module.

===== База данных

Используйте одну из следующих СУБД:

* **MySQL 8.0+** (или совместимые MariaDB 10.6+, Percona 8.0+) с основным движком хранения InnoDB
* **PostgreSQL 16** или выше с расширением pg_trgm
* **SQLite 3.45** или выше. Временные локальные демо-сайты используют SQLite, который включён в PHP. Убедитесь, что версия PHP содержит нужную версию SQLite.

==== Дополнительные материалы

* https://www.drupal.org/docs/system-requirements[_Drupal.org_: System requirements]
* https://secure.php.net/manual/en/configuration.php[PHP Runtime Configuration (memory_limit и другие параметры)]
* https://www.drupal.org/docs/official_docs/en/_local_development_guide.html[Установка Drupal на локальной машине]
* https://www.drupal.org/docs/official_docs/en/_evaluator_guide.html[Создание демо-сайта Drupal для ознакомления]

*Авторы*

Адаптировано https://www.drupal.org/u/KarlKedrovsky[Karl Kedrovsky],
https://www.drupal.org/u/bemery987[Brian Emery], и
https://www.drupal.org/u/jhodgdon[Jennifer Hodgdon], и
https://www.drupal.org/u/eojthebrave[Joe Shindelar] из https://drupalize.me[Drupalize.Me],
на основе https://www.drupal.org/docs/system-requirements["System requirements"],
авторские права 2000-copyright_upper_year за отдельными участниками
https://www.drupal.org/documentation[Drupal Community Documentation].

Переведено https://www.drupal.org/u/levmyshkin[Абраменко Иван] из https://drupalbook.org/ru[DrupalBook].
